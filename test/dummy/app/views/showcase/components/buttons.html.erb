<div class="min-h-screen bg-gray-50 py-12">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <%= link_to "← Back to Components", root_path, class: "text-blue-600 hover:text-blue-800 mb-8 inline-block" %>
    
    <h1 class="text-3xl font-bold text-gray-900 mb-8">Buttons</h1>

    <div class="space-y-12">
      <!-- Button Variants -->
      <section class="bg-white rounded-lg shadow p-6">
        <h2 class="text-xl font-semibold text-gray-900 mb-4">Button Variants</h2>
        <div class="flex flex-wrap gap-4">
          <%= ui.button_to "Primary Button", "#", variant: :primary %>
          <%= ui.button_to "Secondary Button", "#", variant: :secondary %>
          <%= ui.button_to "Danger Button", "#", variant: :danger %>
          <%= ui.button_to "Ghost Button", "#", variant: :ghost %>
        </div>
      </section>

      <!-- Button Sizes -->
      <section class="bg-white rounded-lg shadow p-6">
        <h2 class="text-xl font-semibold text-gray-900 mb-4">Button Sizes</h2>
        <div class="flex flex-wrap items-center gap-4">
          <%= ui.button_to "Small", "#", size: :small %>
          <%= ui.button_to "Medium", "#", size: :medium %>
          <%= ui.button_to "Large", "#", size: :large %>
        </div>
      </section>

      <!-- Button States -->
      <section class="bg-white rounded-lg shadow p-6">
        <h2 class="text-xl font-semibold text-gray-900 mb-4">Button States</h2>
        <div class="flex flex-wrap gap-4">
          <%= ui.button_to "Normal", "#" %>
          <%= ui.button_to "Disabled", "#", disabled: true %>
          <%= ui.button_to "Loading...", "#", "data-turbo-submitting": true %>
        </div>
      </section>

      <!-- Code Example -->
      <section class="bg-white rounded-lg shadow p-6">
        <h2 class="text-xl font-semibold text-gray-900 mb-4">Usage Example</h2>
        <pre class="bg-gray-900 text-gray-100 p-4 rounded overflow-x-auto"><code>&lt;%= ui.button_to "Click me", some_path, variant: :primary, size: :medium %&gt;

&lt;%= ui.button_to "Submit", "#", variant: :primary, data: { turbo_method: :post } %&gt;

&lt;%= ui.button_to "Delete", "#", variant: :danger,
    data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %&gt;</code></pre>
      </section>
    </div>
  </div>
</div>